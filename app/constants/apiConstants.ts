const API_BASE_URL = "http://localhost:3001/moderateApi";

const API_ENDPOINTS = {
  LOGIN: `${API_BASE_URL}/auth/login`,
  JOKES: `${API_BASE_URL}/jokes`,
};

export { API_BASE_URL, API_ENDPOINTS };
